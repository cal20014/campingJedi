export default productDetails(productId, selector)

function addProductToCart(product) {
    setLocalStorage("so-cart", product);
  }

function productDetailsTemplate(product) 

let productData = {}

